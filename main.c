#include <stdio.h>

#include "p_player.h"
#include "g_game_state.h"

void GameLoop(){
    printf("Hello World!\n");
}

int main() {
    // init engine
    // keyboard i/o
    // events handler
    // renderer
    // player
    // rendering context (window)
    GameLoop();
    return 0;
}