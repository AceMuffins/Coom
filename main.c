#include <stdio.h>

void GameLoop(){
    printf("Hello World!\n");
}

int main() {
    // init engine
    // keyboard i/o
    // events handler
    // renderer
    // player
    // rendering context (window)
    GameLoop();
    return 0;
}